使用 jadx 查看算法

        for (byte cur : md5byte) {
            if (cur > (byte) 102 || cur < (byte) 97) {
                result = result + (cur - 48);
            } else {
                result = result + ((cur - 97) + 10);
            }
        }
        if (result.equals("1192811610815159146852912439081023130161513")) {
            return true;
        }

题目将字串的 hex 转换成十进制比较，拆分并转回十六进制即可
